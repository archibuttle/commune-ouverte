---
interface Props {
  maire: {
    nom: string;
    prenom: string;
    sexe: string;
    debut_mandat: string;
  } | null;
}

const { maire } = Astro.props;

function formatDate(iso: string): string {
  const d = new Date(iso);
  return d.toLocaleDateString("fr-FR", { month: "long", year: "numeric" });
}

const title = maire?.sexe === "F" ? "Maire sortante" : "Maire sortant";
---
{maire && (
  <div class="bg-white rounded-xl p-5 shadow-sm border border-neutral-200">
    <div class="text-xs font-medium text-neutral-500 uppercase tracking-wide mb-2">{title}</div>
    <div class="text-lg font-semibold text-neutral-900">
      {maire.prenom} {maire.nom}
    </div>
    <div class="text-sm text-neutral-500 mt-1">
      En fonction depuis {formatDate(maire.debut_mandat)}
    </div>
  </div>
)}
